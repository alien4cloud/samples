tosca_definitions_version: tosca_simple_yaml_1_0_0_wd03
description: TOSCA simple profile with Apache.

node_types:
  alien.nodes.Apache:
    derived_from: tosca.nodes.WebServer
    description: >
      The TOSCA Apache Node Type represents an apache component
      that can be managed and run by a TOSCA Compute Node Type.
    capabilities:
      host:
        type: alien.capabilities.ApacheContainer
        properties:
          valid_node_types: [ tosca.nodes.WebApplication ]
    tags:
      icon: /images/apache.png
    properties:
      version:
        type: version
        default: 2.4
        constraints:
          - equal: 2.4
      port:
        type: integer
        description: Port for the Apache server
        default: 80
        constraints:
          - greater_or_equal: 1
      document_root:
          type: string
          required: true
          default: "/var/www"
    interfaces:
      Standard:
        create: scripts/apache_install.groovy
        start: scripts/start_apache.groovy
      fastconnect.cloudify.extensions:
        start_detection: scripts/apache_start_detection.groovy
    artifacts:
      - scripts: scripts
        type: tosca.artifacts.File

capability_types:
  alien.capabilities.ApacheContainer:
      derived_from: tosca.capabilities.Container

artifact_types:
  tosca.artifacts.GroovyScript:
    description: A groovy script (.groovy file)
    file_ext: [groovy]
